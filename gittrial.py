# addition of two numbers program
def add_two_numbers(a: int, b: int) -> int:
    return a + b
        # nonemployee compensation (Box 7)
        amount = None 
        if is_nonemployee:
            amount = calculate_nonemployee_compensation()
        return amount
        m = re.search(r'box\s*1[:\)]?\s*([^\n\r]+)', text, flags=re.IGNORECASE)
        if m:
            wages = _number_re.search(m.group(1))
            if wages: wages = wages.group(0)                        

